{
  "scripts": {
    "test": "echo \"Error: no test especified\" && exit 1",
    "build-css": "node-sass --include-path scss scss/origen.scss style.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\" "
  },
  "devDependencies": {
    "node-sass": "^6.0.1",
    "nodemon": "^2.0.15"
  }
}
